# ============================================================
# SmartStore 智能无人商店 - 项目依赖文件
# ============================================================
# Python 版本: 3.10+
# CUDA 版本: 11.8 (GPU 加速，可选)
# 导出日期: 2025-10-21
# 环境来源: E:\Anaconda\envs\smart_store
# 
# 安装说明：
#   1. conda create -n smart_store python=3.10 -y
#   2. conda activate smart_store
#   3. pip install torch==2.1.0+cu118 torchvision==0.16.0+cu118 --index-url https://download.pytorch.org/whl/cu118
#   4. pip install -r requirements.txt
#
# ============================================================

# ==================== Web 框架 ====================
Flask==3.0.0
flask-cors==6.0.1
Werkzeug==3.0.1
Jinja2==3.1.6

# ==================== 深度学习框架 ====================
# 注意：PyTorch 需要单独安装 GPU 版本（见上方安装说明）
# torch==2.1.0+cu118
# torchvision==0.16.0+cu118

# TensorFlow (支持 GPU 加速)
tensorflow==2.15.0
tf-keras==2.15.0
keras==2.15.0

# ==================== 计算机视觉 ====================
opencv-python==4.8.1.78
opencv-contrib-python==4.8.1.78
Pillow==10.1.0
numpy==1.24.3

# ==================== 目标检测 - YOLO ====================
ultralytics==8.0.196

# ==================== 人脸识别 - DeepFace ====================
deepface==0.0.95
# 注意：DeepFace 首次运行时会自动下载预训练模型：
#   - Facenet (92MB)
#   - VGG-Face (500MB)
#   - ArcFace (130MB)

# ==================== 数据库 ====================
PyMySQL==1.1.0
SQLAlchemy==2.0.23
cryptography==41.0.7

# ==================== 语音识别与合成 ====================
SpeechRecognition==3.10.0
edge-tts==6.1.9
# 可选的语音处理库（如需要本地音频处理）
# pydub==0.25.1
# soundfile==0.12.1
# sounddevice==0.4.6

# ==================== LLM 与自然语言处理 ====================
openai==1.3.7
# transformers==4.35.2  # 如需本地 LLM
# tiktoken==0.5.2
# jieba==0.42.1  # 中文分词

# ==================== 数据处理 ====================
pandas==2.1.3
numpy==1.24.3  # 核心数值计算库

# ==================== 图像处理（可选） ====================
# scikit-image==0.22.0
# matplotlib==3.8.2
# scipy==1.11.4

# ==================== 配置与工具 ====================
python-dotenv==1.0.0
pydantic==2.5.2
pydantic-settings==2.1.0
requests==2.31.0
PyYAML==6.0.1
tqdm==4.66.1

# ==================== 异步支持 ====================
aiohttp==3.9.1

# ==================== 其他工具 ====================
typing-extensions==4.9.0
#
# 2. Protobuf 版本冲突：
#    - TensorFlow 2.15.0 要求: protobuf>=3.20.3,<5.0.0
#    - 解决方案: 使用 protobuf==4.23.4
#
# 3. PyTorch vs TensorFlow：
#    - 两者可以共存，但建议分别使用
#    - PyTorch 用于 YOLO 和其他 CV 任务
#    - TensorFlow 专用于 DeepFace 人脸识别
#
# 4. CUDA 版本要求：
#    - PyTorch 2.1.0 + CUDA 11.8
#    - TensorFlow 2.15.0 + CUDA 11.8 (通过 tensorflow[and-cuda])
#    - 确保系统已安装 CUDA 11.8 和 cuDNN 8.6+
#
# 5. 已移除的冲突库：
#    - face-recognition (需要 dlib，难以安装)
#    - dlib (需要 C++ 编译器)
#
# ==================== 安装顺序（重要！）====================
#
# 步骤 1: 安装 CUDA 和 cuDNN
#   - CUDA 11.8: https://developer.nvidia.com/cuda-11-8-0-download-archive
#   - cuDNN 8.6+: https://developer.nvidia.com/cudnn
#
# 步骤 2: 创建虚拟环境
#   python -m venv venv
#   venv\Scripts\activate
#
# 步骤 3: 升级 pip
#   python -m pip install --upgrade pip
#
# 步骤 4: 安装 PyTorch (CUDA 11.8)
#   pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu118
#
# 步骤 5: 安装其他依赖
#   pip install -r requirements.txt
#
# 步骤 6: 验证安装
#   python -c "import torch; print(f'PyTorch: {torch.__version__}, CUDA: {torch.cuda.is_available()}')"
#   python -c "import tensorflow as tf; print(f'TensorFlow: {tf.__version__}, GPU: {tf.config.list_physical_devices(\"GPU\")}')"
#   python -c "from deepface import DeepFace; print('DeepFace OK')"
#
# ==================== 国内镜像加速 ====================
#
# 清华镜像：
#   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
#
# 阿里镜像：
#   pip install -i https://mirrors.aliyun.com/pypi/simple/ -r requirements.txt
#
# ==================== 常见问题 ====================
#
# Q1: numpy 版本冲突？
# A: 严格使用 numpy==1.24.3，不要升级
#
# Q2: TensorFlow 找不到 CUDA？
# A: 检查环境变量 PATH 是否包含 CUDA 路径
#
# Q3: PyTorch 找不到 CUDA？
# A: 确认安装的是 cu118 版本，不是 CPU 版本
#
# Q4: DeepFace 下载模型失败？
# A: 手动下载模型到 ~/.deepface/weights/
#
# Q5: 内存不足？
# A: 降低 batch_size，或使用更小的模型（如 OpenFace）
#
# ================================================
